from typing import Dict, List
def run_scenarios(base_cta: List[Dict], scenarios: List[Dict]) -> Dict:
    # TODO: apply patches, run per-twin, compute deltas
    return {"by_scenario": [], "deltas": {}}
